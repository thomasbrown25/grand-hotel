import './rooms-btn.styles.css';

const RoomsBtn = () => {
    return (
        <div className='rooms-btn-wrapper'>
            <button className='rooms-btn'>Check All Rooms</button>
        </div>
    );
};

export default RoomsBtn;
